<Window x:Class="WPF.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF.Views"
        xmlns:vm="clr-namespace:Core.ViewModels;assembly=Core"
        mc:Ignorable="d"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        d:DataContext="{d:DesignInstance Type=vm:MainWindowViewModel}"
        Title="MainWindow" Height="250" Width="600" Style="{StaticResource MaterialDesignWindow}">
    <Grid VerticalAlignment="Center">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Margin="5,0,0,0" VerticalAlignment="Center" Text="Count Value in UI (Min 5) (Max 10)"/>
        <materialDesign:NumericUpDown Grid.Row="0" Grid.Column="1" Minimum="5" Maximum="10" Value="{Binding Count, Mode=TwoWay}"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Margin="5,30,5,5" VerticalAlignment="Center" Text="Count Value in ViewModel"/>
        <TextBlock Grid.Row="1" Grid.Column="1" Margin="5,30,5,5" VerticalAlignment="Center" Text="{Binding Count, Mode=OneWay}"/>

        <Button Grid.Row="2" Grid.Column="0"  Margin="5,50,5,5" Content="Set out of range value from viewModel" Command="{Binding SetInvalidCountCommand}"/>
        <TextBlock Grid.Row="2" Grid.Column="1" Margin="5,50,5,5" VerticalAlignment="Center" Text="Or set out range value manually from UI"/>

    </Grid> 
</Window>
